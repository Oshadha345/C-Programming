#include <stdio.h>
#include <stdlib.h>
#include "file1.h"


int main() {
    int sum_result = fun_sum(5, 10);

    printf("The sum is: %d\n", sum_result);
    // printf("The subtraction is: %d\n", sub_result);
    return 0;
}